{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://liquidinterfaces.org/schemas/0.3.0/execute-payload.json",
  "x-lip-version": "0.3.0",
  "description": "Payload for ``message_type='execute'``.\n\nThe coordinator authorising one step, and telling the agent what that\nstep was authorised for.",
  "properties": {
    "execution_plan": {
      "additionalProperties": true,
      "title": "Execution Plan",
      "type": "object"
    },
    "authorized_scopes": {
      "description": "What this step may do, after every narrowing: the requester's own authority, what the interaction claimed, and what this agent's capability was bound to. An agent MUST NOT assume it holds anything not listed here.",
      "items": {
        "type": "string"
      },
      "title": "Authorized Scopes",
      "type": "array"
    },
    "agent_capability_id": {
      "default": "",
      "description": "The agent's own capability this step executes \u2014 the id it published at register, e.g. `crm.find_customers`. An agent that publishes several capabilities uses this to know which declared `input_model` the context must match. Distinct from `capability_id`, which reference coordinators populate with the session's IBAC capability.",
      "title": "Agent Capability Id",
      "type": "string"
    },
    "capability_id": {
      "default": "",
      "description": "Which of the agent's capabilities is being executed. Echoed back on the completion so the coordinator can check the artifact against the offer that promised its shape \u2014 one agent can hold several accepted capabilities in a session.",
      "title": "Capability Id",
      "type": "string"
    },
    "memory_snapshot": {
      "additionalProperties": true,
      "description": "Session memory this agent is permitted to read.",
      "title": "Memory Snapshot",
      "type": "object"
    }
  },
  "title": "LIP execute payload",
  "type": "object"
}
