{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://liquidinterfaces.org/schemas/0.1.0/offer-payload.json",
  "x-lip-version": "0.1.0",
  "description": "Payload for ``message_type='offer'``.\n\nWhen sent by an individual agent, this describes a single capability.\nWhen sent by the coordinator to the requester, it describes the full\ncomposed execution plan (multiple agents) for the requester to approve,\nreject, or renegotiate before execution begins.",
  "properties": {
    "capability_id": {
      "title": "Capability Id",
      "type": "string"
    },
    "capability_description": {
      "default": "",
      "title": "Capability Description",
      "type": "string"
    },
    "constraints": {
      "additionalProperties": true,
      "title": "Constraints",
      "type": "object"
    },
    "expected_artifacts": {
      "items": {
        "type": "string"
      },
      "title": "Expected Artifacts",
      "type": "array"
    },
    "estimated_cost": {
      "anyOf": [
        {
          "type": "number"
        },
        {
          "type": "null"
        }
      ],
      "default": null,
      "title": "Estimated Cost"
    },
    "estimated_latency": {
      "anyOf": [
        {
          "type": "number"
        },
        {
          "type": "null"
        }
      ],
      "default": null,
      "title": "Estimated Latency"
    },
    "required_scopes": {
      "items": {
        "type": "string"
      },
      "title": "Required Scopes",
      "type": "array"
    },
    "output_schema": {
      "additionalProperties": true,
      "description": "JSON Schema (from ``Model.model_json_schema()``) describing the structured output the agent will produce.  Propagated to the requester via the accept message so it can deserialise results.",
      "title": "Output Schema",
      "type": "object"
    },
    "composition_plan": {
      "additionalProperties": true,
      "description": "When sent by the coordinator, carries the full composed execution plan (steps, agents, topology) for the requester to review before approving or rejecting.",
      "title": "Composition Plan",
      "type": "object"
    },
    "participating_agents": {
      "description": "List of agent IDs participating in the proposed flow.",
      "items": {
        "type": "string"
      },
      "title": "Participating Agents",
      "type": "array"
    }
  },
  "required": [
    "capability_id"
  ],
  "title": "LIP offer payload",
  "type": "object"
}
