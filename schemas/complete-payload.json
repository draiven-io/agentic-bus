{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://liquidinterfaces.org/schemas/0.3.0/complete-payload.json",
  "x-lip-version": "0.3.0",
  "description": "Payload for ``message_type='complete'``.",
  "properties": {
    "status": {
      "default": "success",
      "title": "Status",
      "type": "string"
    },
    "artifacts": {
      "items": {
        "additionalProperties": true,
        "type": "object"
      },
      "title": "Artifacts",
      "type": "array"
    },
    "metadata": {
      "additionalProperties": true,
      "title": "Metadata",
      "type": "object"
    },
    "memory_writes": {
      "additionalProperties": true,
      "description": "Writes this execution staged for the session's shared memory, applied by the coordinator through the agent's write policy. A key outside that policy is refused and audited, not dropped. The coordinator has read this field since session memory existed; it was never declared here, so an implementer reading the schema could not know to send it.",
      "title": "Memory Writes",
      "type": "object"
    }
  },
  "title": "LIP complete payload",
  "type": "object"
}
